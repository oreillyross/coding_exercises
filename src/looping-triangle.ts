function drawTriangles() {
  const array = Array(7).fill("#");
  array.map((triangle, idx) => {
    const line = () => "#" + console.log(triangle, idx);
    return triangle;
  });
}

drawTriangles();
